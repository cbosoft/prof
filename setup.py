#!/usr/bin/env python

from distutils.core import setup

setup(name='prof',
        version='0.1',
        description='próf: (icelandic) verb, to test.',
        author='Chris Boyle',
        packages=['prof'])
